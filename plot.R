# ------------------------------------------------------------------------------
# plot.R
# ------------------------------------------------------------------------------
# Covariants project.
# Plot a map of the world with covid data choropleths.
# ------------------------------------------------------------------------------
# Pete Arnold
# 5 December 2021
# ------------------------------------------------------------------------------

# Plot a map of the data specified.
# Parameters:
# @param    df          data-frame with country name, code and map data.
# @param    variable    string containing the name of the variable to plot.
# Return value:
# @return               The ggplot object containing the map.
# Typical usage:
#   plot_cases <- create_map(covid_variants, 'Case_rate')
#   plot_cases
create_map <- function(df, variable){
    # Write some code to:
    # 1. Plot the data in a world map (straightfoward).
    # 2. Use the specified variable (more advanced).
}
